# Latency benchmark results

Generated by `python3 experiments/latency/bench.py` at `2026-09-23T19:16:41.359682+10:00`.

## Verdict

**Mixed. The current matcher keeps fixture-mode overhead small enough that expensive-tool gates can pay for themselves, but cheap-tool gating is negative and live Jev latency will move the break-even sharply upward.**

The honest result is not "gate everything". Fixture-mode gates are net-positive only when they stop expensive or failure-prone work: `bash`, `task`, `web_search`, duplicate calls, and browser retry loops. They are net-negative for cheap tools such as `edit`, `view`, `glob`, and `rg` unless the gate is a local microsecond prefilter such as `unbounded_large_read()`.

All Jev gate numbers here are **fixture mode**. They exclude real Jev network latency. A live 300-500 ms scorer would be materially worse; the live break-even column below models 400 ms as a midpoint, not as a measurement.

## Measured overhead components

| Component | median ms | p95 ms | n | Notes |
| --- | --- | --- | --- | --- |
| subprocess_python_plus_pre_tool_import | 43.27 | 45.14 | 80 | Fresh interpreter plus executing pre_tool_use.py top-level imports; no payload processing. |
| route_pre_tool_use_not_gated_edit | 0.0005 | 0.0005 | 20000 | Direct harness.route_pre_tool_use() None path for a cheap tool. |
| violates_read_only_policy_check | 0.0020 | 0.0021 | 20000 | Pure local read-only policy predicate used before any Jev call. |
| external_write_decide_source_policy | 0.0977 | 0.1113 | 5000 | Full gate-local hard-rule decision; source=policy and zero network. |
| tool_worth_it_full_gate_source_fixture | 0.2694 | 0.3150 | 2000 | Dynamic gate load + config load + fixture replay + evaluate(); no subprocess. |
| unbounded_large_read_large_file | 0.0607 | 0.0699 | 10000 | Local file-size prefilter on a 1000-line file; implementation counts to 601 lines. |
| unbounded_large_read_small_file | 0.0426 | 0.0526 | 10000 | Same prefilter on a small file that should not be gated. |

### End-to-end hook cost

| Hook path | median ms | p95 ms | n | Notes |
| --- | --- | --- | --- | --- |
| hook_e2e_cheap_tool_allow | 40.30 | 48.04 | 80 | Actual preToolUse subprocess with JSON stdin; cheap tool returns allow without a gate. |
| hook_e2e_readonly_policy_deny | 40.36 | 45.17 | 80 | Actual preToolUse subprocess; read-only write violation denied locally before Jev. |
| hook_e2e_fixture_gate_bash | 86.18 | 89.07 | 80 | Actual preToolUse subprocess; bash routes to tool-worth-it with source=fixture. |
| hook_e2e_large_view_fixture_gate | 87.76 | 90.10 | 50 | Actual preToolUse subprocess; large unbounded view routes to context-read-budget fixture. |
| post_failure_retry_fixture_gate | 87.39 | 90.51 | 80 | Actual postToolUseFailure subprocess; browser click failure routes to retry-worth-it fixture. |

## Session-level model

The current `hooks.json` preToolUse matcher is narrower than "all tools": it matches expensive tools and external-write/read-only integrations, not `edit`, `view`, `glob`, `rg`, `sql`, etc. So the startup tax is paid on matched calls, not literally all 3,970 tool calls in this telemetry. The all-tools line shows the cost if that matcher were widened.

`harness.py` currently defines `CHEAP_TOOLS=['create', 'edit', 'glob', 'grep', 'str_replace', 'view']` and `EXPENSIVE_TOOLS=['bash', 'run_factory', 'task', 'web_fetch', 'web_search']`. The measured matcher is `bash|task|run_factory|web_fetch|web_search|create_pull_request|update_pull_request|create_issue|add_pr_review_comment|reply_to_comment|reply_and_resolve_review_thread|send_session_message|create_issue_artifact|slack-.*|workiq-.*|atlassian-.*|cc-828GDeeqlf0t1i29-.*|cc-pdqHAftw9cy3d8Vw-.*|cc-qoxxvytaDqWyRth7-.*`.

| Scenario | 30d hook calls | overhead seconds | seconds/session |
| --- | --- | --- | --- |
| current hooks.json preToolUse | 2168 | 186.84 | 3.1700 |
| postToolUseFailure retry advice | 103 | 9.0000 | 0.1500 |
| current total | 2271 | 195.84 | 3.3200 |
| hypothetical preToolUse on all tools | 4073 | 268.47 | 4.5500 |

## Plausible savings from the same telemetry

| Lever | Telemetry basis | plausible saved seconds | Caveat |
| --- | --- | --- | --- |
| Prevent duplicate `bash` calls | 77 wasted repeats × 9.107 s | 701.24 | Cleanest defensible latency win; redundant gate is not wired into current preToolUse state. |
| Prevent known-latency duplicate calls | 210 repeats with avg latency known | 1618.50 | Includes `read_bash`, which may include legitimate polling; treat as directional. |
| Replace bash read/search/list with built-ins | 831 bash calls doing read/search/list | 7021.95 | Upper bound. Current hook can identify this but does not rewrite bash to rg/view/glob. |
| Avoid duplicate browser clicks | 4 identical repeats × 3.762 s | 15.05 | postToolUseFailure can advise, not block. |
| Avoid one `task` | task average latency | 174.36 | One correct block pays most of the 30-day fixture overhead. |
| Avoid one `web_search` | web_search average latency | 38.96 | Useful, but only 10 calls in the window. |

## Break-even

With the current matcher and fixture-mode measurements, total modeled hook overhead is **195.84 s over 30 days** (**3.32 s/session**). That is paid back by **22** prevented duplicate `bash` calls out of 77 available, or **2** avoided `task` calls, or **6** avoided `web_search` calls. Browser-click retry advice alone would need **53** avoided click retries, but only 4 identical browser-click repeats were observed, so it cannot carry the whole system by itself.

| Saved tool/action | avg saved ms | fixture break-even correct % | live 400ms break-even correct % |
| --- | --- | --- | --- |
| edit | 256 | 33.66 | 171.99 |
| view | 908 | 9.4900 | 48.49 |
| web_fetch | 1864 | 4.6200 | 23.62 |
| bash | 9107 | 0.9500 | 4.8300 |
| web_search | 38961 | 0.2200 | 1.1300 |
| task | 174362 | 0.0500 | 0.2500 |
| playwright-browser_click | 3762 | 2.3200 | 11.70 |

Read the table bluntly: fixture-mode gating of `bash`, `web_search`, and `task` only needs to be right occasionally. Live gating of `web_fetch` needs to be right often. Live gating of `edit` is mathematically hopeless because the scorer costs more than the tool.

## Assumptions and limitations

- `TYPESAFE_API_KEY` was unset and `JEV_MODE=fixture`; no live Jev network call was made.

- Tool-level counts come from the aggregate cloud queries checked into `telemetry/queries/07-09`; the local baseline JSON contains model/session aggregates but not those rows.

- The model assumes the current `hooks.json` matcher. If preToolUse is installed globally for every tool, use the all-tools scenario.

- `unbounded_large_read()` is described as a local stat path, but the current implementation opens the file and counts until line 601; it is still microsecond-scale in this benchmark.

- Fixture replay uses local JSON and is much faster than a real remote scorer. Live Jev latency must be measured before claiming production wall-clock gains.

## Recommendations outside `experiments/latency/`

- Do not widen the preToolUse matcher to cheap tools; keep cheap tools behind local prefilters only.

- Wire `redundant-tool-call` into real hook state if duplicate-call savings are a goal; the telemetry says duplicate `bash` alone can pay for the fixture overhead.

- Make `context-read-budget` able to block or rewrite `bash` read/search/list calls; today it can detect them but `to_decision()` only rewrites large `view` calls.

- Consider adding pre-call coverage for slow/failure-prone Kusto tools if policy allows; current hooks only advise after Kusto failures.
